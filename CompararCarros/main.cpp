#include <iostream>
#include <string>

using namespace std;

int main() {
  char resp;
  char entrada;
  int AnoCarro;
  float Velocidade, VelocidadeTotal;
  int contador = 0;

  while (true) {
    cout << "Teste\n";
    cin >> resp;
    cin >> AnoCarro;
    cin >> Velocidade;
    cin >> resp;
    contador += 1;
  }
}
